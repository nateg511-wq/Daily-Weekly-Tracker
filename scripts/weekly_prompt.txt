Read CLAUDE.md in this directory top to bottom — it is the authoritative, self-contained runbook for this project. Do not rely on any memory of past sessions.

Run this week's FULL cycle exactly as CLAUDE.md's "update cycle" section describes (Steps 1-6), not the light daily path:

1. Quant refresh: `./.venv/Scripts/python.exe run.py quant` then `./.venv/Scripts/python.exe run.py numbers`.
2. Full qualitative research pass on all four pillars (CLARITY Act, Iran War Risk, RWA Tokenization, Fed & Macro) — what changed since the previous score set (check `note` fields/dates in config/scores.json and each report's "What Changed" section). Follow Research Discipline rules exactly: primary sources first (federalreserve.gov, congress.gov, bls.gov, dol.gov, treasury.gov, federalregister.gov, CENTCOM releases, exchange APIs, rwa.xyz, prediction markets) then 2+ independent outlets; date every fact; label single-source claims as single-source; never conflate market-price odds with institutional estimates; check every trigger table; re-verify the previous report's facts still hold.
3. Re-rate: add a new dated score set in config/scores.json (key format YYYY-MM-DD_draft, status "draft", one-line note), set active_qual_set to it. Move P only as far as evidence moved with a rationale per changed window. Enforce the sufficiency rule (single-source claims may be reported but never alone move a P). Never touch Quality, the uniform Impact in config/settings.json, or the equal-weight model. Never edit or rename any prior (non-today) score set, and never set status to "committed" or add "stamped_by" — stamping is owner (Alex) only.
4. Blend: `./.venv/Scripts/python.exe run.py blend`.
5. Update all reports in reports/current/*.html to reflect the research — keep the existing HTML template/CSS exactly, edit content only, house section order as specified in CLAUDE.md. Update every date stamp (runfoot, cover "Updated & Re-verified", decision stamp) to today; leave provenance dates ("Written...", "committed...") untouched. Update the full lineage column in the scores table. Work through the entire "Field checklist per report" list in CLAUDE.md so every number, call, and stamp is consistent everywhere it's quoted.
6. Render: `run.py render`. Selfcheck: `run.py selfcheck`.
7. Verify like money depends on it per CLAUDE.md's Verification list: recompute a couple of signals by hand and spot-check in the PDFs, re-check every new fact against its source, cross-check the same fact quoted in multiple reports, check PDFs page by page for rendering breakage. If you have subagent capability, run independent adversarial verification of new claims against primary sources (not the report's own citations) before finishing.
8. Only after verification passes: archive (`run.py archive`), using a same-date `-2` suffix if an archive for today already exists. Never prune or overwrite existing archives.
9. Commit all changes to git with a clear, descriptive commit message.
10. End by printing a summary: today's date, the new score set key, what moved and why (one line per pillar), THE FINAL NUMBER (equal-weight and horizon-weighted) and the leave-one-out range, and anything that needs the owner's (Alex's) attention before stamping.

If you have no working internet/web research capability in this environment, do not invent a score change — just run the quant refresh, commit if anything changed, and say so plainly in your summary.
